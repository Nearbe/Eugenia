# Testing Context: Eugenia Conventions

## Core Strategy
The testing strategy is centered on **Mathematical Invariant Verification**. Instead of just checking outputs, tests are designed to ensure that implemented mathematical operators strictly adhere to the algebraic and topological axioms defined in the `Universe` theory.

## Key Patterns
- **Algebraic Property Checks**: Verifying identities such as $d \circ h = id$ or complex conjugation properties (e.g., `test_d_h_are_inverse_on_scalars`).
- **State Continuity & Stability**: Testing that operations like branching and compression preserve fundamental invariants (e.g., `test_complex_fullness_is_stable_under_branching`).
- **Topological/Fractal Verification**: Using geometric primitives to ensure scaling laws and fractal dimensions hold within the implemented models.

## Tools & Conventions
- **`pytest`**: The primary test runner.
- **`pytest-mpl`**: Used for verifying visual invariants in matplotlib visualizations (e.g., checking if a rendered fractal or trajectory matches its theoretical shape).
- **Strict Typing**: Tests often rely on the strict type system (`ComplexState`, etc.) to prevent invalid mathematical state transitions.

## Example Invariants Checked
- `dual_add(a, b)` must satisfy identity/associative axioms.
- `branch(state)` must follow defined growth velocity patterns.
- `compress(state)` must preserve the information depth signature.

---
*Generated by Eugenia Research Agent*
